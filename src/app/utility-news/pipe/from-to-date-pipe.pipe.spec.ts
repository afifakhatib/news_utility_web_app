import { FromToDatePipePipe } from './from-to-date-pipe.pipe';

describe('FromToDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FromToDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
